## Cosa cambia

<!-- Una o due frasi. Il "perché" conta più del "cosa". -->

## Perché

<!-- Quale problema reale risolve. Se c'è una issue, linkala. -->

## Verifiche

- [ ] `npx tsc --noEmit` pulito
- [ ] `npx jest --silent` verde
- [ ] `cd web && npm test` verde (se hai toccato la web app)
- [ ] Ho aggiunto o aggiornato i test dove il comportamento cambia

## Soldi altrui

<!-- Lascia vuoto se non c'entra. Se il tuo cambiamento tocca il layer AI: -->

- [ ] Ogni nuova chiamata AI risolve lo scope della gilda giusta
- [ ] Nessun percorso nuovo può far pagare a un tavolo le chiamate di un altro
- [ ] Nessuna credenziale in chiaro fuori da `Secret.reveal()`
